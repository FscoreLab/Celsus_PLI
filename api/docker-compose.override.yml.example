# Пример docker-compose.override.yml для локальной разработки
# Скопируйте этот файл в docker-compose.override.yml и раскомментируйте нужные volume mounts

version: '3.8'

services:
  ct-clip-api:
    volumes:
      # Опционально: монтировать локальные модели для быстрой итерации
      # - ../best_models:/app/best_models
      
      # Опционально: монтировать локальный код для разработки без rebuild
      # - ../api:/app/api
      # - ../CT_CLIP:/app/CT_CLIP
      # - ../diffusion_anomaly:/app/diffusion_anomaly
      
      # Опционально: локальная папка с DICOM архивами для тестирования
      # - /path/to/your/dicom/archives:/app/dicom_archives

